import{useRef,useState}from'react';import{Circle,CornerDownRight,Eraser,Minus,MousePointer2,Pen,Square,Trash2,Type}from'lucide-react';export function Whiteboard(){const canvas=useRef<HTMLCanvasElement>(null);const[drawing,setDrawing]=useState(false);const pos=(e:React.PointerEvent<HTMLCanvasElement>)=>{const r=e.currentTarget.getBoundingClientRect();return{x:e.clientX-r.left,y:e.clientY-r.top}};const start=(e:React.PointerEvent<HTMLCanvasElement>)=>{const c=canvas.current?.getContext('2d');if(!c)return;const p=pos(e);c.beginPath();c.moveTo(p.x,p.y);c.strokeStyle='#a78bfa';c.lineWidth=3;setDrawing(true);e.currentTarget.setPointerCapture(e.pointerId)};const move=(e:React.PointerEvent<HTMLCanvasElement>)=>{if(!drawing)return;const c=canvas.current?.getContext('2d');const p=pos(e);c?.lineTo(p.x,p.y);c?.stroke()};return <div className="whiteboard"><div className="whiteboard-tools">{[[MousePointer2,'Select'],[Pen,'Pen'],[Square,'Rectangle'],[Circle,'Circle'],[CornerDownRight,'Arrow'],[Type,'Text'],[Eraser,'Eraser']].map(([Icon,label])=><button title={String(label)} key={String(label)}><Icon/></button>)}<span/><button title="Clear" onClick={()=>{const c=canvas.current;c?.getContext('2d')?.clearRect(0,0,c.width,c.height)}}><Trash2/></button></div><canvas ref={canvas} width={900} height={600} onPointerDown={start} onPointerMove={move} onPointerUp={()=>setDrawing(false)}/><div className="board-seed"><strong>filter()</strong><span>input array</span><Minus/><span>callback</span><Minus/><span>new array</span></div></div>}
