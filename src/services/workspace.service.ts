import{productRepository as repo}from'../mocks/product.repository';export const assignmentService={list:repo.assignments,submit:async(_id:string,_payload:{repositoryUrl?:string;code?:string})=>({success:true as const})};export const paymentService={transactions:repo.transactions,removeMethod:async(_id:string)=>({success:true as const})};export const adminUiService={users:repo.users};
