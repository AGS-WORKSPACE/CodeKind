import{useCallback,useEffect,useState}from'react';const KEY='pairlore:favourite-tutors';export function useFavourites(){const[ids,setIds]=useState<string[]>(()=>{try{return JSON.parse(localStorage.getItem(KEY)??'[]')as string[]}catch{return[]}});useEffect(()=>localStorage.setItem(KEY,JSON.stringify(ids)),[ids]);const toggle=useCallback((id:string)=>setIds(current=>current.includes(id)?current.filter(x=>x!==id):[...current,id]),[]);return{ids,isFavourite:(id:string)=>ids.includes(id),toggle}}
